// ServicesOverview removed — this company no longer offers professional learning/training tracks.
// The file is retained as an inert placeholder to avoid accidental import errors during incremental edits.

export const ServicesOverview = () => {
  return null;
};
